.Side
  .Side__username
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icon
        =link_to new_group_path(current_user) , class: "icon icon__edit" do
          = icon('far', 'edit' )
        =link_to edit_user_path(current_user), class: "icon icon__cog" do
          = icon('fas', 'cog' )
  .Side__grouplist
    - current_user.groups.each do |group|
      .group-box
        = link_to group_messages_path(group) do
          .group-box__tittle
            = group.name
          .group-box__message
            = group.show_last_message
    